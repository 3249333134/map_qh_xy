---
name: Map Q
description: 地图之上的乳白毛玻璃与圆润小组件
colors:
  primary: "#202020"
  primary-strong: "#101010"
  primary-soft: "#ededeb"
  publish: "#f56f64"
  page: "#eef3f4"
  surface: "#ffffff"
  surface-soft: "#eaf0ef"
  text: "#202020"
  text-body: "#595959"
  text-muted: "#62716e"
  border: "#dce6e3"
typography:
  body:
    fontFamily: "-apple-system, BlinkMacSystemFont, Segoe UI, PingFang SC, Microsoft YaHei, Helvetica Neue, Arial, sans-serif"
    fontWeight: 400
    lineHeight: 1.5
  title:
    fontWeight: 700
rounded:
  control: "16px"
  card: "22px"
  panel: "64rpx"
  pill: "999rpx"
---

# Map Q 视觉规范

## 方向与边界

2026-09-11 根据用户提供的三张参考图，采用乳白毛玻璃、圆润小组件与柔和悬浮层次，替代之前偏实色的极简皮肤。地图和内容为主角，沿用系统中文字体、深色正文与珊瑚色发布入口。

保留页面分区、模块顺序、地图与面板比例、五项底部导航、拖拽定位、滚动区域、安全区及操作逻辑。样式调整不涉及接口、事件、路由与本地数据。既有详情主题及业务语义颜色继续保留。

## 材质与层次

- 背景使用微冷的浅灰绿色 #eef3f4；卡片采用接近实色的乳白底。
- 毛玻璃用于地图面板、导航和发布工作区：24px 模糊、135% 饱和度、白色内高光与柔和偏移阴影。
- 共享玻璃底默认为 86% 不透明度，支持模糊时增强至 76%；微信独立导航使用 92% 乳白底作为保守回退。真实地图模糊效果仍需在有地图 Key 的环境和小程序真机验证。
- 内容卡片使用 22px 圆角，个人足迹卡片使用 40rpx 圆角。长列表卡片不逐个添加模糊，避免滚动开销。
- 选中分类使用浅薄荷色和深绿文字，个人页分段选项使用白色凸起块；主要提交操作继续使用深色底白字。
- 珊瑚色发布入口、照片、头像、地图路线及状态色保留。

## 控件与适配

- 首页搜索与工具继续保持 44px，圆角调整为 18px；不改变外部定位及点击事件。
- 卡片标题继续使用 14px，辅助文字 11px，保留原有信息密度、卡片间距和内容排布。
- 消息会话使用圆角浅色条目，保留频道栏和滑动操作；发布功能卡使用独立白色小组件。
- 保留按压、选中、禁用、键盘焦点及减少动态效果支持；不向拖拽面板增加新的位移动画。
- 横屏仍使用现有左右分区规则，底部导航保留原有高度与安全区计算。

## 实现约定

主题入口为 `map_q/static/app-theme.css`，局部材质在原组件样式内更新；不扩展全局强制覆盖范围。小程序独立导航同时更新 `custom-tab-bar/index.wxss`。后续改动优先复用颜色、圆角及阴影变量。

### 消息与个人页比例统一（2026-09-11）

消息页采用整块乳白玻璃面板，默认顶部为屏幕 42%；个人页默认位置采用 42% 并保留既有拖拽边界。两页主切换为 44px、13px 文字、16px 圆角，薄荷色表示选中。消息条目使用轻分隔线，取消独立卡片阴影；新建菜单按五项内容自然收高。日期区使用透明底与文字操作，月历独立滚动；足迹卡片缩减封面占位和内边距。保留所有事件绑定、导航和数据操作。


### 分类型内容与服务卡片（2026-09-12）

文章采用较浅封面与三行摘要；视频采用纵向媒体区域；地点强调实景、位置与导航；路线强调距离、时长和途经点；活动突出时间与报名；服务突出真实评分、价格、营业状态和预约。共享媒体高度通过 --card-media-height 调整，保持原有双列布局与地图面板拖拽逻辑。

详情页使用大媒体、22–30px 圆角信息区与毛玻璃操作区；服务保留原有深浅主题，地图内详情保留时段、途经点和预约流程。媒体缺失使用文字占位。服务列表按内容类型选用卡片，并以点击卡片数据定位与跳转，避免列内索引导致详情错位；服务卡取消随机评分并按服务数据的公里单位显示距离。


### 全屏视频与固定抽屉（2026-09-12）

视频详情采用沉浸式深色画布、右侧互动按钮、底部作者与进度。点击列表视频及播放按钮统一进入详情；上滑下一条，下滑上一条。评论与地点共用固定抽屉，按安全区内可用高度分配上方视频 40%、下方抽屉 60%，无可拖动分界线；展开与互切不暂停或重建视频，展开时禁用视频切换。地图支持拖动缩放与回到标注地点，不以当前位置代替缺失标注。

推荐流沿用现有内容接口与演示数据开关，不传首页地图筛选；使用独立分页并按 ID 去重。每条内容独立保存播放进度、互动和分享状态。真机验收需补充有效视频源与地图配置，检查原生媒体、地图、软键盘和安全区兼容。

视频视觉细化：使用统一实心图标与头像关注角标、居中小号导航、轻量进度条；地点信息改为地图上的乳白毛玻璃悬浮卡片，评论／地点切换使用细下划线，保持固定 40%／60% 比例。缺少真实媒体时展示明确空态，不使用无关视频替代。


### 视频播放稳定性与横屏展示（2026-09-16）

横屏视频保留完整画面，以同一视频提取的封面模糊铺底，仅一个播放器。抽屉仍为视频 40%／内容 60%，展开不重建播放器。演示视频使用固定 ID 与素材版本；分页请求复用，播放事件按当前播放实例隔离，返回或离页取消待执行切换。只有无源或真实错误才出现错误提示，缓冲保留封面。
