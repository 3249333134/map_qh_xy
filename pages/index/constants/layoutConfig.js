/**
 * 布局配置常量
 */
export const LAYOUT_CONFIG = {
  // 拖动手柄高度
  DRAG_HANDLE_HEIGHT: 40,
  
  // 边距
  MARGIN: 20,
  
  // 内容区域初始高度比例（相对于屏幕高度）
  INITIAL_CONTENT_RATIO: 0.33,
  
  // 内容区域最大高度比例（相对于屏幕高度）
  MAX_CONTENT_RATIO: 0.75,
  
  // 吸附阈值（低）
  SNAP_THRESHOLD_LOW: 0.3,
  
  // 吸附阈值（高）
  SNAP_THRESHOLD_HIGH: 0.7,
  
  // 滚动与高度变化比例
  SCROLL_HEIGHT_RATIO: 3,
  
  // 滚动锁定持续时间（毫秒）
  SCROLL_LOCK_DURATION: 300
}